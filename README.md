# hybrid-ml-recommendation-platform
A multi-service system where a Node.js backend orchestrates a Python-based ML inference service to run recommendation experiments, with support for caching, pagination, and failure handling.
